{
  "title": "Spiron RPC Overview",
  "panels": [
    { "type": "graph", "title": "RPC Broadcast Rate", "targets": [ { "expr": "sum(rate(spiron_rpc_broadcast_total[5m])) by (peer)" } ] },
    { "type": "graph", "title": "RPC Failures", "targets": [ { "expr": "sum(rate(spiron_rpc_failures_total[5m])) by (peer)" } ] },
    { "type": "graph", "title": "Commit Latency P90", "targets": [ { "expr": "histogram_quantile(0.9, sum(rate(spiron_rpc_latency_bucket[5m])) by (le, peer))" } ] }
  ]
}
